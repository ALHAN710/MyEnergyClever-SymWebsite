{% extends 'home.html.twig' %}

{% block title %} Edition d'un Site  {% endblock %}

{% block row_x_title %}{% endblock %}

{% form_theme form 'site/_collection.html.twig' %} 

{% block body %}
    <div class="container">
        <h1>Modifier le Site : {{ site.name }}</h1>
        
        {{ form_start(form) }}
            {#  <div class="form-group">
                {{ form_label(form.title) }}
                {{ form_widget(form.title, {'attr':{'class': 'form-control', 'placeholder':'Titre de l\'annonce'}}) }}
            </div> #}
            {{form_widget(form)}}

            <button type = "submit" class="btn btn-primary rounded-0">Enregistrer les modifications</button>
        {{ form_end(form) }}
        

       {#} {{form(form)}} #}
    </div>

{% endblock %}



{#{% block form_row %}
  
    <div class="form-group">
        {{form_label(form)}}
        {{form_widget(form)}}
    </div>
{% endblock %}

{% block text_widget %}
    <input type="text" name="{{full_name}}" id="{{id}}" class="form-control">
{% endblock %} 
#}
{#  {{dump()}} #}

{% block javascripts %}
    <script src="/js/site.js"> </script>
{% endblock %}